(function ( app ) {

	app.facade = {

		define : function ( core, module ) {
			
			return {
				subscribe : function () {
					core.
				}
				publish : function () {
					core.
				}
			}

		}

	}

})( app );